const fs = require('fs');  let content = fs.readFileSync('electron/sniper.ts', 'utf8');  content = content.replace(/this\.executeSnipeWorkflow\(task\)\.catch/g, `(task.strategy === 'FEED' ? this.executeFeedSnipe(task) : this.executeDirectSnipe(task)).catch`); content = content.replace(/private async executeSnipeWorkflow/g, 'private async executeDirectSnipe');  fs.writeFileSync('electron/sniper.ts', content); console.log("Replaced workflow call");